extends layout

block get

  - var data = #{data} || [{}]

  if errors
    ul.my-errors
      for error in errors
        li= error.msg

h1(class="text-success")
  br
  p Auteur: data.author
  br
  figure
    img(src=data.body, alt="photo à editer")
    figcaption data.title
  form(method="delete", action="/image/edit")
    input(hidden=data.id, name="id")
    button(class="text-secondary", type="submit") Modifier
  form(method="delete", action="/image/delete")
     input(hidden=data.id, name="id")
     button(class="text-danger", type="submit") Supprimer

